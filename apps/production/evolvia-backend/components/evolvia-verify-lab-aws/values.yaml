namespace:
  name: evolvia-backend-prod

image:
  repository: ghcr.io/cloudsteak/evolvia-verify-lab-aws
  tag: "1f038a2"
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  type: ClusterIP
  port: 8000

config:
  PROVIDER: aws
  LOG_LEVEL: INFO

imagePullSecrets:
  enabled: true
  name: ghcr-auth

probes:
  periodSeconds: 15
